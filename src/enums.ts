export enum State {
	Pending = 'pending',
	Assigned = 'assigned', 
	InTransit = 'in_transit', 
	Delivered = 'delivered'
}

export enum Status {
	Available = 'available',
	Busy = 'busy', 
	Reserved = 'reserved'
}